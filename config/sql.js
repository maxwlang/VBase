/* * * * MySQL (! >= 8.0) || MariaDB Configuration * * * */
const sql = {

    // Database hostname or IP address
    host: 'localhost',

    // Database port
    port: 3306,

    // Database username
    user: 'VBase',

    // Database password
    password: 'password',

    // Database for the site
    database: 'VBase',
};

module.exports = sql;
