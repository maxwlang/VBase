/* * * * Session configuration * * * */
const session = {
    
    // The session key name
    key: 'vbase_cookie',

    // The session secret
    // If set to %VBASE_UNIQUE% in single quotes, VBase will generate a unique key on first run.
    secret: '40068a6d-cc0e-4d46-826d-85b5738a2677',
};

module.exports = session;
